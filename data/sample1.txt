# yolo

import Foo/Bar as FB
import Foo/Biz
from Foo.Baz import (foo, bar, baz)
from Foo.Baz import (foo)

foo :: Star, Power -> Magic; # yolo

bar :: Sparkle, Power -> Magic where {
      pickle
    ; foo
  };

x = 5;

x = (foo 5);

x = foo (bar 4) (baz 6.5);

source R
  
  library(magrittr)
  runif(10) %>%
    sum

source python
    
    def foo(x):
        x * 2
